{
  "name": "web-terminal",
  "version": "1.0.0",
  "description": "Web Terminal Project",
  "main": "index.js",
  "scripts": {
    "start": "live-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"Add build scripts if needed\""
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "xterm": "^4.14.0",
    "xterm-addon-fit": "^0.2.2",
    "live-server": "^1.2.1"
  }
}
